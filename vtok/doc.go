// Package vtok provides provider-neutral text tokenization helpers.
//
// The package requires provider injection. It does not import dictionaries,
// segment text, rank keywords, open network connections, read credentials, or
// touch local filesystem paths by default.
package vtok
